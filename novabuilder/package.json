{
  "name": "novabuilder",
  "version": "0.1.0",
  "description": "NovaBuilder visual editor",
  "main": "build/index.js",
  "scripts": {
    "build": "node -e \"console.log('Build skipped; using prebuilt assets in build/.')\"",
    "start": "node -e \"console.log('Dev server not configured; use prebuilt assets in build/.')\""
  }
}
